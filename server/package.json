{
  "name": "server",
  "version": "1.0.0",
  "description": "",
  "main": "app.js",
  "scripts": {
    "start": "node server.js",
    "dev": "SET NODE_ENV=development && nodemon -r dotenv/config server.js dotenv_config_path=./.env.development ",
    "test": "jest --watchAll --setupFiles",
    "seed": "SET NODE_ENV=development && node -r dotenv/config db/index.js dotenv_config_path=./.env.development"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "dotenv": "^16.0.3",
    "express": "^4.18.2",
    "http-errors": "^2.0.0",
    "pg": "^8.10.0"
  },
  "devDependencies": {
    "jest": "^29.5.0",
    "nodemon": "^2.0.21",
    "supertest": "^6.3.3"
  }
}
